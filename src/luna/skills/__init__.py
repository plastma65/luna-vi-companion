"""Registry skill của Luna (Giai đoạn 2).

Khi viết skill mới, import và đăng ký vào REGISTRY theo docs/SKILL.md.
Hiện để trống — sẽ điền ở GĐ2.
"""

# Ví dụ khi tới GĐ2:
# from .web_search import WebSearchSkill
# REGISTRY = {s.name: s for s in [WebSearchSkill()]}
REGISTRY: dict = {}
